<% include partials/header %>

<div class="container">
<header class="jumbotron">
    <div class = "container">
        <h1 class="display-3">Welcome to TruGeo</h1>
            <h4>Your solution for bulk map queries!</h4>
            <p class="header-info">
                    <% if(!currentUser){ %>
                        <a class = "btn btn-primary btn-lg" href="/login">Login</a>
                        <a class = "btn btn-primary btn-lg" href="/register">Register</a>
                        <p>Login or Register to get started!<p>
                    <% } else { %>
                        <a class = "btn btn-primary btn-lg" href="/dashboard"><i class="fas fa-glasses"></i> Dashboard</a>
                        <a class = "btn btn-primary btn-lg" href="/locations"><i class="fas fa-plus"></i> Search a location</a>
                        <a class = "btn btn-secondary btn-lg" href="/logout"><i class="fas fa-sign-out-alt"></i> Upload a list of locations</a>   
                    <% } %>
            </p>
    </div>
</header>

<div class = "row">
    <div class = "col-lg-12">
        <!--<h3>Welcome!</h3>-->
    </div>
</div>

</div>

<% include partials/footer %>